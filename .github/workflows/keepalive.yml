name: Keep Hugging Face Space Alive

on:
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping HF Space
        run: |
          curl -L --max-time 30 --retry 3 --retry-delay 5 https://aiasmin-openai-cpa.hf.space || true
